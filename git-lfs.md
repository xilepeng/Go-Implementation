
## Homebrew: brew install git-lfs


```go
git lfs install

git lfs track "*.mp4"

git add .gitattributes

```


```shell
➜  ~ brew install git-lfs
Running `brew update --preinstall`...
Warning: git-lfs 3.0.2 is already installed and up-to-date.
To reinstall 3.0.2, run:
  brew reinstall git-lfs

➜  ~ git lfs install
Updated git hooks.
Git LFS initialized.
➜  ~ git lfs track "*.mp4"
Tracking "*.mp4"
➜  ~ git add .gitattributes
```